global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: final-lab-api
    metrics_path: /metrics
    static_configs:
      - targets: ["127.0.0.1:8081"]

# vLLM is intentionally not declared here until its actual metrics address has
# been verified for the selected Lab 01 runtime and network topology.
