# Workload data

This directory is intentionally empty. Add only non-sensitive, versioned
benchmark prompts or workload definitions after documenting their provenance.
Do not commit user prompts, secrets, production logs, model outputs containing
sensitive data, or generated benchmark results.
